// Mock system config. Replace with env vars (NEXT_PUBLIC_*) or
// GET /api/config endpoint when backend is available.
export const configSistema = {
  whatsappSistema: "+52 55 1234 5678",
  correoSistema: "documentos@centur.saptiva.com",
};
